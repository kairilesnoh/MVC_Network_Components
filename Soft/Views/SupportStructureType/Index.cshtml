@using Nc.Facade.Network.ComponentType
@using Nc.Pages.Controls
@model IEnumerable<Nc.Facade.Network.ComponentType.SupportStructureTypeViewModel>

@{
    ViewData["Title"] = "Support structure type";
Layout = "_Index";
var properties = new ShowTableProperties<SupportStructureTypeViewModel>(ViewBag);
properties.Items = Model;
}

@Html.ShowTable(properties)



