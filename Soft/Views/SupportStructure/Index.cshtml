@using Nc.Facade.Network.Components
@using Nc.Pages.Controls
@model IEnumerable<Nc.Facade.Network.Components.SupportStructureViewModel>

@{
    ViewData["Title"] = "Support Structure";
Layout = "_Index";
var properties = new ShowTableProperties<SupportStructureViewModel>(ViewBag);
properties.Items = Model;
}

@Html.ShowTable(properties)



