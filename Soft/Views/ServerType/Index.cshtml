@using Nc.Facade.Network.ComponentType
@using Nc.Pages.Controls
@model IEnumerable<Nc.Facade.Network.ComponentType.ServerTypeViewModel>

@{
    ViewData["Title"] = "Network server type";
Layout = "_Index";
var properties = new ShowTableProperties<ServerTypeViewModel>(ViewBag);
properties.Items = Model;
properties.ExcludedProperties = new string[] { "Switches" };
}

@Html.ShowTable(properties)



